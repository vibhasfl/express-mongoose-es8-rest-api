version: "2"
services:
  app:
    container_name: nodejsmongoosees8restapi
    build: .
    ports:
      - "4000:8081"
    depends_on:
      -  mongodb
    environment:
      -  MONGO_HOSTNAME=mongodb
  mongodb:
    container_name: mongodb
    image: mongo
    ports:
      - "27018:27017"
